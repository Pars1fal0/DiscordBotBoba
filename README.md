# Discord Bot Arvent

> Многофункциональный Discord-бот с модерацией, логированием и удобными информационными командами.

## 📑 Содержание
- [✨ Основные возможности](#-основные-возможности)
- [📦 Установка](#-установка)
- [🔐 Настройка переменных окружения](#-настройка-переменных-окружения)
- [⚙️ Конфигурационные файлы](#-конфигурационные-файлы)
- [🚀 Запуск](#-запуск)
- [🛠️ Структура проекта](#-структура-проекта)
- [📚 Полезные команды](#-полезные-команды)
- [🧩 Подключение новых когов](#-подключение-новых-когов)
- [🤝 Вклад в проект](#-вклад-в-проект)
- [📄 Лицензия](#-лицензия)

## ✨ Основные возможности
- **Модульная архитектура** — каждый набор команд вынесен в отдельный ког (расширение) в каталоге [`cogs/`](cogs), что упрощает поддержку и кастомизацию.
- **Модерация** — команды `!ban`, `!unban`, `!kick`, `!mute` и `!unmute` помогают поддерживать порядок на сервере.
- **Информационные карточки** — `!help`, `!sinfo`, `!uinfo` выводят красиво оформленные Embed-сообщения о сервере, пользователях и доступных командах.
- **Продвинутое логирование** — ког [`advanced_logging.py`](cogs/advanced_logging.py) отслеживает удаление и редактирование сообщений, изменения участников и голосовых событий, записывая их в выбранный канал.
- **Настраиваемый онбординг** — приветственные каналы и роли задаются в отдельном JSON-файле, а бот может отправлять приветственные сообщения новым участникам.
- **Безопасный запуск** — параметры (токен, ID владельца) читаются из переменных окружения или файла `.env` посредством `python-dotenv`.

## 📦 Установка
1. Склонируйте репозиторий и перейдите в каталог проекта:
   ```bash
   git clone https://github.com/your-username/DiscordBotBoba.git
   cd DiscordBotBoba
   ```
2. Создайте и активируйте виртуальное окружение (рекомендуется):
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # Windows: .venv\Scripts\activate
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

> Совет: при необходимости обновите `pip` командой `python -m pip install --upgrade pip` перед установкой зависимостей.

## 🔐 Настройка переменных окружения
Создайте файл `.env` в корне проекта и заполните его значениями:
```env
DISCORD_TOKEN=ваш_бот_токен
OWNER_ID=ваш_ID_дискорда
```
> Совет: получайте токен только с [портала разработчика Discord](https://discord.com/developers/applications). Храните файл `.env` вне системы контроля версий.

## ⚙️ Конфигурационные файлы
В корне проекта присутствует несколько JSON-файлов, задающих дополнительные настройки:

| Файл | Назначение |
|------|------------|
| `logging_config.json` | Каналы для отправки логов об удалении, изменении сообщений и других событиях. |
| `moderation_config.json` | Права и параметры модерации, в том числе роли с доступом к командам. |
| `welcome_channels.json` | Соответствие серверов и каналов для приветственных сообщений. |
| `mutes.json` | Актуальный список заглушенных пользователей, используется для восстановления статуса после рестарта бота. |
| `warnings.json` | Сохранение предупреждений, выданных модераторами. |

При первом запуске вы можете оставить файлы пустыми — бот создаст недостающие структуры автоматически. Для изменения настроек отредактируйте соответствующие поля и перезапустите бота.

## 🚀 Запуск
После настройки достаточно запустить бота командой:
```bash
python main.py
```
При успешном старте в консоли появится информация о загруженных когах и серверах, к которым подключён бот.

## 🛠️ Структура проекта
```
.
├── cogs/                  # Набор расширений с командами и событиями
│   ├── help.py            # Кастомная команда !help
│   ├── sinfo.py           # Информация о сервере
│   ├── uinfo.py           # Информация о пользователе
│   ├── ban.py, kick.py    # Модерация участников
│   ├── mute.py            # Временная блокировка участника
│   ├── logging.py         # Базовое логирование событий
│   └── advanced_logging.py# Расширенное логирование с настройками
├── main.py                # Точка входа, инициализация бота и автозагрузка когов
├── requirements.txt       # Список Python-зависимостей
└── README.md              # Это описание проекта
```

## 📚 Полезные команды
| Команда | Описание |
|---------|----------|
| `!help` | Список доступных команд с кратким описанием. |
| `!sinfo` | Информация о текущем сервере: владелец, количество участников, даты создания и т. д. |
| `!uinfo` | Персональная карточка пользователя с статусом, датой регистрации и пингом. |
| `!ban <@пользователь> [причина]` | Забанить участника, автоматически уведомив его в личные сообщения. |
| `!unban <id/имя>` | Гибкий разбан по ID, упоминанию или имени (включая частичные совпадения). |
| `!kick <@пользователь> [причина]` | Вежливо уведомить и удалить пользователя с сервера. |
| `!mute <@пользователь> [время]` | Отключить возможность писать/говорить на заданный срок. |
| `!unmute <@пользователь>` | Снять ограничения с участника. |

## 🧩 Подключение новых когов
1. Создайте файл в папке `cogs/` и добавьте класс-наследник `commands.Cog` с командами.
2. В конце файла не забудьте определить асинхронную функцию `setup(bot)`, которая добавит ког:  
   ```python
   async def setup(bot):
       await bot.add_cog(MyCog(bot))
   ```
3. При следующем запуске бот автоматически найдёт и подключит новый ког.

## 🤝 Вклад в проект
Будем рады вашим идеям и улучшениям! Перед отправкой Pull Request:
1. Откройте issue с описанием предложения или проблемы.
2. Создайте отдельную ветку для изменений.
3. Убедитесь, что код оформлен в стиле PEP 8 и протестирован локально.

## 📄 Лицензия
Проект распространяется по лицензии [MIT](LICENSE.txt). Используйте, изменяйте и распространяйте свободно с сохранением указания авторства.

---
Если у вас есть вопросы или вы хотите внести вклад — смело открывайте issue или пишите владельцу бота в Discord!
